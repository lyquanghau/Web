mixin search(keyword)
    form(id="form-seach")
        .input-group 
            input(
                type="text"
                placeholder="Tìm kiếm sản phẩm..."
                name="keyword"
                class="form-control"
                value=keyword
            )
            .input-group-append 
                button(
                    class="btn btn-success"
                    type="submit"
                ) Tìm
